<!DOCTYPE html>

<!-- Track 2: Navigation & Efficiency Enhancements Implemented -->
<!-- Features: -->
<!-- ① Command Palette (Cmd+K) with Fuse.js fuzzy search -->
<!-- ② Global Keyboard Shortcuts (Cmd+S, ?, g+d/c/m) -->
<!-- ③ Client-side Breadcrumbs from URL paths -->
<!-- ④ Toast Notification System with HTMX integration -->
<!-- ⑤ Recent Items tracking in Command Palette -->
<!-- ⑥ Keyboard Shortcuts Help Modal -->

<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#ffffff">
    <title>
        #if(title): #(title) - #endifSwiftCMS Admin
    </title>

    <!-- CSS & Frameworks -->
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.7.2/dist/full.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        dark: {
                            900: '#111827',
                            800: '#1f2937',
                            700: '#374151',
                            600: '#4b5563',
                        }
                    }
                }
            }
        }
    </script>

    <!-- JavaScript Libraries -->
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.3/dist/cdn.min.js"></script>

    <!-- Additional CDN Libraries -->
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/fuse.js@7.0.0"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tiptap/core@2.1.0/dist/tiptap-core.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tiptap/starter-kit@2.1.0/dist/tiptap-starter-kit.min.js"></script>

    <!-- Custom Styles -->
    <style>
        /* Theme transition for smooth switching */
        html {
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* Dark mode specific styles */
        [data-theme="dark"] {
            color-scheme: dark;
        }

        [data-theme="light"] {
            color-scheme: light;
        }

        .sidebar-transition {
            transition: transform 0.3s ease-in-out, margin-left 0.3s ease-in-out;
        }
        .content-transition {
            transition: margin-left 0.3s ease-in-out;
        }
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .nav-item {
            position: relative;
        }
        .nav-item.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 3px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 0 3px 3px 0;
        }

        /* Dark mode overrides for specific components */
        [data-theme="dark"] .table th {
            background-color: #1f2937;
        }

        [data-theme="dark"] .badge-ghost {
            background-color: #374151;
            color: #d1d5db;
        }

        [data-theme="dark"] input,
        [data-theme="dark"] select,
        [data-theme="dark"] textarea {
            background-color: #1f2937;
            border-color: #374151;
            color: #f3f4f6;
        }

        [data-theme="dark"] input:focus,
        [data-theme="dark"] select:focus,
        [data-theme="dark"] textarea:focus {
            border-color: #667eea;
            outline-color: #667eea;
        }

        [data-theme="dark"] .dropdown-content {
            background-color: #1f2937;
            border-color: #374151;
        }

        [data-theme="dark"] .menu-title {
            color: #9ca3af;
        }

        /* Scrollbar styling for dark mode */
        [data-theme="dark"] ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        [data-theme="dark"] ::-webkit-scrollbar-track {
            background: #1f2937;
        }

        [data-theme="dark"] ::-webkit-scrollbar-thumb {
            background: #4b5563;
            border-radius: 4px;
        }

        [data-theme="dark"] ::-webkit-scrollbar-thumb:hover {
            background: #6b7280;
        }

        /* Code blocks in dark mode */
        [data-theme="dark"] pre,
        [data-theme="dark"] code {
            background-color: #1f2937;
            color: #e5e7eb;
        }

        /* Modal and dialog backgrounds */
        [data-theme="dark"] .modal-box {
            background-color: #1f2937;
        }

        /* Toast/alert backgrounds */
        [data-theme="dark"] .alert {
            background-color: #1f2937;
            border-color: #374151;
        }

        /* Icon color adaptation */
        [data-theme="dark"] svg {
            color: inherit;
        }

        /* Image contrast adjustments */
        [data-theme="dark"] img {
            opacity: 0.9;
        }

        [data-theme="dark"] img:hover {
            opacity: 1;
        }

        /* Skeleton loading states */
        .skeleton-pulse {
            animation: skeleton-pulse 1.5s ease-in-out infinite;
            background: linear-gradient(90deg, oklch(var(--b3)) 25%, oklch(var(--b2)) 50%, oklch(var(--b3)) 75%);
            background-size: 200% 100%;
        }
        @keyframes skeleton-pulse {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
        }

        /* Enhanced card hover effects */
        .card, .stat-card {
            transition: all 0.2s ease;
            border: 1px solid transparent;
        }
        .card:hover, .stat-card:hover {
            border-color: oklch(var(--p) / 0.2);
        }

        /* Command palette backdrop */
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="bg-base-200 min-h-screen" x-data="adminApp()">

    <!-- Mobile Menu Toggle Button -->
    <div class="lg:hidden fixed top-4 left-4 z-50">
        <button @click="toggleSidebar()" class="btn btn-circle btn-primary shadow-lg">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
        </button>
    </div>

    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <aside
            class="sidebar sidebar-transition fixed lg:static inset-y-0 left-0 z-40 w-64 bg-base-100 border-r border-base-300 flex flex-col
                   lg:translate-x-0 lg:ml-0"
            :class="{ '-translate-x-full': !isSidebarOpen }"
            @click.away="if (window.innerWidth < 1024) isSidebarOpen = false">

            <!-- Sidebar Header -->
            <div class="p-6 border-b border-base-300">
                <div class="flex items-center justify-between">
                    <div>
                        <h1 class="text-2xl font-bold gradient-text">SwiftCMS</h1>
                        <p class="text-sm text-base-content/60 mt-1">Headless CMS</p>
                    </div>
                    <button @click="toggleTheme()"
                            class="btn btn-circle btn-ghost btn-sm group"
                            :title="isDarkMode ? 'Switch to Light Mode' : 'Switch to Dark Mode'"
                            x-transition:enter="transition ease-out duration-200"
                            x-transition:enter-start="opacity-0 scale-75"
                            x-transition:enter-end="opacity-100 scale-100">
                        <!-- Moon Icon (shown in light mode) -->
                        <svg x-show="!isDarkMode"
                             x-transition:enter="transition ease-out duration-200"
                             x-transition:enter-start="opacity-0 rotate-12"
                             x-transition:enter-end="opacity-100 rotate-0"
                             x-transition:leave="transition ease-in duration-150"
                             x-transition:leave-start="opacity-100 rotate-0"
                             x-transition:leave-end="opacity-0 rotate-12"
                             xmlns="http://www.w3.org/2000/svg"
                             class="h-5 w-5 text-base-content group-hover:scale-110 transition-transform"
                             fill="none"
                             viewBox="0 0 24 24"
                             stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                        </svg>
                        <!-- Sun Icon (shown in dark mode) -->
                        <svg x-show="isDarkMode"
                             x-transition:enter="transition ease-out duration-200"
                             x-transition:enter-start="opacity-0 rotate-12"
                             x-transition:enter-end="opacity-100 rotate-0"
                             x-transition:leave="transition ease-in duration-150"
                             x-transition:leave-start="opacity-100 rotate-0"
                             x-transition:leave-end="opacity-0 rotate-12"
                             xmlns="http://www.w3.org/2000/svg"
                             class="h-5 w-5 text-base-content group-hover:scale-110 transition-transform"
                             fill="none"
                             viewBox="0 0 24 24"
                             stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Sidebar Navigation -->
            <nav class="flex-1 p-4 overflow-y-auto">
                <ul class="menu menu-vertical gap-1">
                    <li class="nav-item #if(activePage == "dashboard") active #endif">
                        <a href="/admin" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "dashboard") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
                            </svg>
                            <span class="font-medium">Dashboard</span>
                        </a>
                    </li>

                    <li class="nav-item #if(activePage == "content-types") active #endif">
                        <a href="/admin/content-types" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "content-types") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"/>
                            </svg>
                            <span class="font-medium">Content Types</span>
                        </a>
                    </li>

                    <li class="nav-item #if(activePage == "content") active #endif">
                        <a href="/admin/content" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "content") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h8a2 2 0 012 2v12a2 2 0 01-2 2H6a2 2 0 01-2-2V4zm3 1h6v8H7V5zm8 10v2H5v-2h10z" clip-rule="evenodd"/>
                            </svg>
                            <span class="font-medium">Content</span>
                        </a>
                    </li>

                    <li class="nav-item #if(activePage == "media") active #endif">
                        <a href="/admin/media" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "media") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd"/>
                            </svg>
                            <span class="font-medium">Media Library</span>
                        </a>
                    </li>

                    <li class="nav-item #if(activePage == "users") active #endif">
                        <a href="/admin/users" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "users") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path d="M9 6a3 3 0 11-6 0 3 3 0 016 0zM17 6a3 3 0 11-6 0 3 3 0 016 0zM12.93 17c.046-.327.07-.66.07-1a6.97 6.97 0 00-1.5-4.33A5 5 0 0119 16v1h-6.07zM6 11a5 5 0 015 5v1H1v-1a5 5 0 015-5z"/>
                            </svg>
                            <span class="font-medium">Users & Roles</span>
                        </a>
                    </li>

                    <div class="divider my-4"></div>

                    <li class="nav-item #if(activePage == "webhooks") active #endif">
                        <a href="/admin/webhooks" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "webhooks") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M12.586 4.586a2 2 0 112.828 2.828l-3 3a2 2 0 01-2.828 0 1 1 0 00-1.414 1.414 4 4 0 005.656 0l3-3a4 4 0 00-5.656-5.656l-1.5 1.5a1 1 0 101.414 1.414l1.5-1.5zm-5 5a2 2 0 012.828 0 1 1 0 101.414-1.414 4 4 0 00-5.656 0l-3 3a4 4 0 105.656 5.656l1.5-1.5a1 1 0 10-1.414-1.414l-1.5 1.5a2 2 0 11-2.828-2.828l3-3z" clip-rule="evenodd"/>
                            </svg>
                            <span class="font-medium">Webhooks</span>
                        </a>
                    </li>

                    <li class="nav-item #if(activePage == "settings") active #endif">
                        <a href="/admin/settings" class="flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-primary hover:text-primary-content transition-colors #if(activePage == "settings") bg-primary bg-opacity-10 text-primary #endif">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M11.49 3.17c-.38-1.56-2.6-1.56-2.98 0a1.532 1.532 0 01-2.286.948c-1.372-.836-2.942.734-2.106 2.106.54.886.061 2.042-.947 2.287-1.561.379-1.561 2.6 0 2.978a1.532 1.532 0 01.947 2.287c-.836 1.372.734 2.942 2.106 2.106a1.532 1.532 0 012.287.947c.379 1.561 2.6 1.561 2.978 0a1.533 1.533 0 012.287-.947c1.372.836 2.942-.734 2.106-2.106a1.533 1.533 0 01.947-2.287c1.561-.379 1.561-2.6 0-2.978a1.532 1.532 0 01-.947-2.287c.836-1.372-.734-2.942-2.106-2.106a1.532 1.532 0 01-2.287-.947zM10 13a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd"/>
                            </svg>
                            <span class="font-medium">Settings</span>
                        </a>
                    </li>
                </ul>
            </nav>

            <!-- Sidebar Footer -->
            <div class="p-4 border-t border-base-300">
                <div class="text-xs text-base-content/60">
                    <p>SwiftCMS v1.0.0</p>
                    <p class="mt-1">© 2024 SwiftCMS</p>
                </div>
            </div>
        </aside>

        <!-- Overlay for mobile -->
        <div
            class="fixed inset-0 bg-black bg-opacity-50 z-30 lg:hidden"
            x-show="isSidebarOpen"
            @click="isSidebarOpen = false"
            x-transition:enter="transition ease-in-out duration-300"
            x-transition:enter-start="opacity-0"
            x-transition:enter-end="opacity-100"
            x-transition:leave="transition ease-in-out duration-300"
            x-transition:leave-start="opacity-100"
            x-transition:leave-end="opacity-0">
        </div>

        <!-- Main Content -->
        <main class="content-area content-transition flex-1 flex flex-col min-h-screen lg:ml-0 ml-0">
            <!-- Sticky Header -->
            <header class="sticky top-0 z-20 bg-base-100 border-b border-base-300">
                <div class="flex items-center justify-between p-6">
                    <!-- Page Title -->
                    <div class="flex items-center gap-4">
                        <h2 class="text-2xl font-bold text-base-content">
                            #if(title): #(title) #else Dashboard #endif
                        </h2>
                        #if(subtitle):
                            <span class="text-base-content/60">| #(subtitle)</span>
                        #endif
                    </div>

                    <!-- Header Actions -->
                    <div class="flex items-center gap-4">
                        <!-- Theme Toggle (Desktop) -->
                        <button @click="toggleTheme()"
                                class="btn btn-circle btn-ghost btn-sm hidden md:flex"
                                :title="isDarkMode ? 'Switch to Light Mode' : 'Switch to Dark Mode'">
                            <svg x-show="!isDarkMode" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                            </svg>
                            <svg x-show="isDarkMode" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                            </svg>
                        </button>

                        <!-- Search -->
                        <div class="form-control hidden sm:block">
                            <div class="relative">
                                <input
                                    type="text"
                                    placeholder="Search..."
                                    class="input input-bordered input-sm w-64 pr-10"
                                    hx-get="/admin/search"
                                    hx-trigger="keyup changed delay:300ms"
                                    hx-target="#search-results"
                                    name="q"
                                    @focus="searchFocused = true"
                                    @blur="searchFocused = false">
                                <svg class="absolute right-3 top-1/2 transform -translate-y-1/2 h-4 w-4 text-base-content/40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                                </svg>
                            </div>
                        </div>

                        <!-- Notifications -->
                        <button class="btn btn-circle btn-ghost btn-sm relative">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
                            </svg>
                            <span class="badge badge-xs badge-primary absolute -top-1 -right-1">3</span>
                        </button>

                        <!-- User Profile Dropdown -->
                        <div class="dropdown dropdown-end">
                            <div tabindex="0" role="button" class="flex items-center gap-3 cursor-pointer hover:bg-base-200 rounded-lg px-3 py-2 transition-colors">
                                <div class="avatar placeholder">
                                    <div class="bg-gradient-to-br from-primary to-secondary text-primary-content rounded-full w-10">
                                        <span class="text-sm font-semibold">
                                            #if(userInitials): #(userInitials) #else AD #endif
                                        </span>
                                    </div>
                                </div>
                                <div class="hidden md:block text-right">
                                    <p class="text-sm font-medium text-base-content">
                                        #if(userName): #(userName) #else Admin User #endif
                                    </p>
                                    <p class="text-xs text-base-content/60">
                                        #if(userRole): #(userRole) #else Administrator #endif
                                    </p>
                                </div>
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-base-content/40" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                                </svg>
                            </div>
                            <ul tabindex="0" class="dropdown-content menu menu-sm bg-base-100 rounded-box z-[51] mt-3 w-64 p-2 shadow-2xl border border-base-300">
                                <li class="menu-title">
                                    <span>Account</span>
                                </li>
                                <li>
                                    <a href="/admin/profile" class="flex items-center gap-3">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                        </svg>
                                        My Profile
                                    </a>
                                </li>
                                <li>
                                    <a href="/admin/preferences" class="flex items-center gap-3">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                        </svg>
                                        Preferences
                                    </a>
                                </li>
                                <li class="menu-title mt-4">
                                    <span>System</span>
                                </li>
                                <li>
                                    <a href="/admin/documentation" target="_blank" class="flex items-center gap-3">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                                        </svg>
                                        Documentation
                                    </a>
                                </li>
                                <li>
                                    <a href="/admin/logout" class="flex items-center gap-3 text-error">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                        </svg>
                                        Sign Out
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- Search Results Dropdown -->
                <div id="search-results"
                     class="absolute top-full left-0 right-0 bg-base-100 border-t border-base-300 shadow-lg z-10"
                     x-show="searchFocused"
                     @click.away="searchFocused = false">
                </div>
            </header>

            <!-- Breadcrumb Navigation -->
            <nav class="breadcrumbs text-sm px-6 py-2 border-b border-base-200" aria-label="Breadcrumb"
                 x-data="{ crumbs: [] }"
                 x-init="
                    const path = window.location.pathname.replace('/admin', '').split('/').filter(Boolean);
                    const labels = {
                        'content-types': 'Content Types', 'content': 'Content', 'media': 'Media Library',
                        'users': 'Users & Roles', 'webhooks': 'Webhooks', 'settings': 'Settings',
                        'new': 'New', 'edit': 'Edit', 'search': 'Search'
                    };
                    let url = '/admin';
                    crumbs = path.map((seg, i) => {
                        url += '/' + seg;
                        return { label: labels[seg] || decodeURIComponent(seg), url: url, isLast: i === path.length - 1 };
                    });
                 ">
                <ul>
                    <li><a href="/admin" class="text-base-content/60 hover:text-primary">Dashboard</a></li>
                    <template x-for="crumb in crumbs" :key="crumb.url">
                        <li>
                            <a x-show="!crumb.isLast" :href="crumb.url" class="text-base-content/60 hover:text-primary" x-text="crumb.label"></a>
                            <span x-show="crumb.isLast" class="text-base-content font-medium" x-text="crumb.label"></span>
                        </li>
                    </template>
                </ul>
            </nav>

            <!-- Main Content Area -->
            <div class="flex-1 p-6 overflow-y-auto">
                <!-- Import Section -->
                #import("content")
            </div>

            <!-- Footer -->
            <footer class="bg-base-100 border-t border-base-300 p-6">
                <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                    <div class="text-sm text-base-content/60">
                        <p>SwiftCMS Admin Panel v1.0.0</p>
                        <p class="mt-1">© 2024 SwiftCMS Team. Open Source Headless CMS.</p>
                    </div>
                    <div class="flex items-center gap-4 text-sm text-base-content/60">
                        <a href="/admin/api-docs" class="hover:text-primary transition-colors">API Documentation</a>
                        <a href="https://github.com/artfularchivesstudio-spec/SwiftCMS" target="_blank" class="hover:text-primary transition-colors">GitHub</a>
                        <a href="/admin/support" class="hover:text-primary transition-colors">Support</a>
                    </div>
                </div>
            </footer>
        </main>
    </div>

    <!-- Alpine.js Component -->
    <script>
        function adminApp() {
            return {
                isDarkMode: false,
                isSidebarOpen: window.innerWidth >= 1024,
                searchFocused: false,
                systemPreference: false,
                toasts: [],
                commandPalette: {
                    isOpen: false,
                    query: '',
                    filteredItems: [],
                    selectedIndex: 0,
                    allItems: [
                        { title: 'Dashboard', url: '/admin', category: 'Page' },
                        { title: 'Content Types', url: '/admin/content-types', category: 'Page' },
                        { title: 'Media Library', url: '/admin/media', category: 'Page' },
                        { title: 'Users & Roles', url: '/admin/users', category: 'Page' },
                        { title: 'Webhooks', url: '/admin/webhooks', category: 'Page' },
                        { title: 'Settings', url: '/admin/settings', category: 'Page' },
                        { title: 'Documentation', url: '/admin/documentation', category: 'Page' },
                        { title: 'API Docs', url: '/admin/api-docs', category: 'Page' },
                        { title: 'New Content Type', url: '/admin/content-types/new', category: 'Action' },
                        { title: 'Toggle Dark Mode', url: null, action: 'toggleTheme()', category: 'Action' },
                        { title: 'Show Keyboard Shortcuts', url: null, action: "document.getElementById('shortcuts-modal').showModal()", category: 'Action' },
                        #for(type in contentTypes):
                        { title: '#(type.displayName)', url: '/admin/content/#(type.slug)', category: 'Content' },
                        { title: 'New #(type.displayName)', url: '/admin/content/#(type.slug)/new', category: 'Action' },
                        #endfor
                    ]
                },
                _pendingGo: false,

                init() {
                    this.initializeTheme();
                    this.commandPalette.filteredItems = this.commandPalette.allItems;
                    this.commandPalette.recentItems = this.loadRecentItems();

                    // Fuse.js fuzzy search instance
                    this._fuse = new Fuse(this.commandPalette.allItems, {
                        keys: ['title', 'category'],
                        threshold: 0.4
                    });

                    // Save shortcut handler with toast feedback
                    window.addEventListener('cms:save', () => {
                        if (typeof window.saveContent === 'function') {
                            window.saveContent()
                                .then(() => this.addToast('Saved successfully', 'success'))
                                .catch(() => this.addToast('Save failed', 'error'));
                        } else {
                            this.addToast('Save not available on this page', 'warning');
                        }
                    });

                    // HTMX event handlers for toasts
                    document.body.addEventListener('htmx:after-request', (event) => {
                        const xhr = event.detail.xhr;
                        const detail = event.detail;

                        if (detail.requestConfig.verb === 'post' || detail.requestConfig.verb === 'put' || detail.requestConfig.verb === 'delete') {
                            if (xhr.status >= 200 && xhr.status < 300) {
                                this.addToast(detail.requestConfig.verb === 'delete' ? 'Deleted successfully' : 'Saved successfully', 'success');
                            } else if (xhr.status >= 400) {
                                this.addToast('Request failed', 'error', 6000);
                            }
                        }
                    });

                    // System theme listener
                    window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
                        this.systemPreference = e.matches;
                        if (!localStorage.getItem('theme')) {
                            this.isDarkMode = e.matches;
                            this.applyTheme();
                        }
                    });

                    // Resize listener
                    window.addEventListener('resize', () => {
                        if (window.innerWidth >= 1024) this.isSidebarOpen = true;
                    });

                    // Cross-tab theme sync
                    window.addEventListener('storage', (e) => {
                        if (e.key === 'theme') {
                            this.isDarkMode = e.newValue === 'dark';
                            this.applyTheme();
                        }
                    });

                    // Global keyboard shortcuts
                    document.addEventListener('keydown', (e) => {
                        // Cmd+K — Command Palette
                        if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
                            e.preventDefault();
                            this.commandPalette.isOpen = !this.commandPalette.isOpen;
                            this.commandPalette.query = '';
                            this.commandPalette.selectedIndex = 0;
                            this.commandPalette.filteredItems = this.commandPalette.allItems;
                            if (this.commandPalette.isOpen) {
                                this.$nextTick(() => this.$refs.cmdInput?.focus());
                            }
                            return;
                        }

                        // Cmd+S — Save (prevent default, dispatch custom event)
                        if ((e.metaKey || e.ctrlKey) && e.key === 's') {
                            e.preventDefault();
                            window.dispatchEvent(new CustomEvent('cms:save'));
                            return;
                        }

                        // Don't fire shortcuts when typing in inputs
                        const tag = e.target.tagName;
                        if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT' || e.target.isContentEditable) return;

                        // ? — Shortcuts help
                        if (e.key === '?' && !e.metaKey && !e.ctrlKey) {
                            e.preventDefault();
                            document.getElementById('shortcuts-modal')?.showModal();
                            return;
                        }

                        // Vim-style g+key navigation
                        if (e.key === 'g' && !this._pendingGo) {
                            this._pendingGo = true;
                            setTimeout(() => { this._pendingGo = false; }, 1000);
                            return;
                        }
                        if (this._pendingGo) {
                            this._pendingGo = false;
                            const goMap = { d: '/admin', c: '/admin/content-types', m: '/admin/media' };
                            if (goMap[e.key]) { window.location.href = goMap[e.key]; return; }
                        }
                    });
                },

                filterCommands() {
                    const q = this.commandPalette.query.trim();
                    if (!q) {
                        // Show recent items first when query is empty
                        this.commandPalette.filteredItems = [...this.commandPalette.recentItems, ...this.commandPalette.allItems]
                            .filter((item, index, self) => index === self.findIndex(t => t.url === item.url && t.title === item.title))
                            .slice(0, 20);
                    } else {
                        this.commandPalette.filteredItems = this._fuse.search(q).map(r => r.item);
                    }
                    this.commandPalette.selectedIndex = 0;
                },

                executeCommand() {
                    const item = this.commandPalette.filteredItems[this.commandPalette.selectedIndex];
                    if (!item) return;
                    this.commandPalette.isOpen = false;

                    if (item.action) {
                        eval(item.action);
                    } else if (item.url) {
                        window.location.href = item.url;
                    }

                    // Add to recent items if not an action
                    if (item.url && !item.action) {
                        this.addRecentItem(item);
                    }
                },

                addRecentItem(item) {
                    // Remove if already exists
                    this.commandPalette.recentItems = this.commandPalette.recentItems.filter(
                        ri => ri.url !== item.url || ri.title !== item.title
                    );

                    // Add to beginning with timestamp
                    this.commandPalette.recentItems.unshift({
                        ...item,
                        accessedAt: Date.now()
                    });

                    // Keep only last 10 items
                    this.commandPalette.recentItems = this.commandPalette.recentItems.slice(0, 10);

                    // Save to localStorage
                    localStorage.setItem('cmdPaletteRecent', JSON.stringify(
                        this.commandPalette.recentItems.map(({title, url, category}) => ({title, url, category}))
                    ));
                },

                loadRecentItems() {
                    try {
                        const saved = localStorage.getItem('cmdPaletteRecent');
                        return saved ? JSON.parse(saved) : [];
                    } catch {
                        return [];
                    }
                },

                addToast(message, type = 'success', duration = 4000) {
                    const id = Date.now();
                    this.toasts.push({ id, message, type, progress: 100 });
                    const interval = setInterval(() => {
                        const toast = this.toasts.find(t => t.id === id);
                        if (toast) {
                            toast.progress -= (100 / (duration / 50));
                            if (toast.progress <= 0) {
                                clearInterval(interval);
                                this.toasts = this.toasts.filter(t => t.id !== id);
                            }
                        } else {
                            clearInterval(interval);
                        }
                    }, 50);
                },

                initializeTheme() {
                    const savedTheme = localStorage.getItem('theme');
                    if (savedTheme) {
                        this.isDarkMode = savedTheme === 'dark';
                    } else {
                        this.systemPreference = window.matchMedia('(prefers-color-scheme: dark)').matches;
                        this.isDarkMode = this.systemPreference;
                    }
                    this.applyTheme();
                },

                applyTheme() {
                    const theme = this.isDarkMode ? 'dark' : 'light';
                    document.documentElement.setAttribute('data-theme', theme);
                    if (this.isDarkMode) {
                        document.documentElement.classList.add('dark');
                    } else {
                        document.documentElement.classList.remove('dark');
                    }
                    const metaThemeColor = document.querySelector('meta[name="theme-color"]');
                    if (metaThemeColor) {
                        metaThemeColor.setAttribute('content', this.isDarkMode ? '#111827' : '#ffffff');
                    }
                },

                toggleTheme() {
                    this.isDarkMode = !this.isDarkMode;
                    localStorage.setItem('theme', this.isDarkMode ? 'dark' : 'light');
                    document.documentElement.style.transition = 'background-color 0.3s ease, color 0.3s ease';
                    this.applyTheme();
                    setTimeout(() => { document.documentElement.style.transition = ''; }, 300);
                },

                setTheme(theme) {
                    this.isDarkMode = theme === 'dark';
                    localStorage.setItem('theme', theme);
                    this.applyTheme();
                },

                toggleSidebar() {
                    this.isSidebarOpen = !this.isSidebarOpen;
                }
            }
        }

        // Initialize theme immediately to prevent flash
        (function() {
            const savedTheme = localStorage.getItem('theme');
            const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            const isDark = savedTheme ? savedTheme === 'dark' : prefersDark;

            document.documentElement.setAttribute('data-theme', isDark ? 'dark' : 'light');
            if (isDark) {
                document.documentElement.classList.add('dark');
            }
        })();
    </script>

    <!-- Command Palette Overlay -->
    <div x-show="commandPalette.isOpen" x-cloak x-transition.opacity.duration.150ms
         class="fixed inset-0 z-[100] bg-black/50 backdrop-blur-sm flex items-start justify-center pt-[20vh]"
         @keydown.escape.window="commandPalette.isOpen = false"
         @click.self="commandPalette.isOpen = false">
        <div class="bg-base-100 rounded-2xl shadow-2xl w-full max-w-lg overflow-hidden border border-base-300"
             @keydown.arrow-down.prevent="commandPalette.selectedIndex = Math.min(commandPalette.selectedIndex + 1, commandPalette.filteredItems.length - 1)"
             @keydown.arrow-up.prevent="commandPalette.selectedIndex = Math.max(commandPalette.selectedIndex - 1, 0)"
             @keydown.enter.prevent="executeCommand()">
            <div class="p-4 border-b border-base-300 flex items-center gap-3">
                <svg class="w-5 h-5 text-base-content/40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
                <input type="text" x-model="commandPalette.query" x-ref="cmdInput"
                       placeholder="Type a command or search..."
                       class="bg-transparent w-full text-lg focus:outline-none"
                       @input="filterCommands()">
            </div>
            <div class="max-h-80 overflow-y-auto p-2">
                <template x-if="commandPalette.filteredItems.length === 0 && commandPalette.query">
                    <div class="p-4 text-center text-base-content/50 text-sm">No results found</div>
                </template>

                <!-- Show recent items when query is empty -->
                <template x-if="!commandPalette.query && commandPalette.recentItems.length > 0">
                    <div>
                        <div class="px-3 py-2 text-xs font-semibold text-base-content/60 uppercase tracking-wider">Recent</div>
                        <template x-for="(item, index) in commandPalette.recentItems.slice(0, 5)" :key="item.title + item.url">
                            <a :href="item.url || '#'" @click="item.action ? (eval(item.action), commandPalette.isOpen = false) : null"
                               class="flex items-center gap-3 p-3 rounded-lg cursor-pointer transition-colors"
                               :class="index === commandPalette.selectedIndex ? 'bg-primary/10 text-primary' : 'hover:bg-base-200'">
                                <span class="text-xs font-mono px-1.5 py-0.5 rounded bg-base-200 text-base-content/60" x-text="item.category"></span>
                                <span class="flex-1 flex items-center gap-2">
                                    <svg x-show="item.url" class="w-4 h-4 text-base-content/40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                    </svg>
                                    <span x-text="item.title"></span>
                                </span>
                                <svg x-show="item.url" class="w-4 h-4 text-base-content/30" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </a>
                        </template>
                    </div>
                </template>

                <!-- Show all results (or filtered) -->
                <template x-if="commandPalette.query || commandPalette.recentItems.length === 0">
                    <div>
                        <template x-for="(item, index) in commandPalette.filteredItems" :key="item.title + item.url">
                            <a :href="item.url || '#'" @click="item.action ? (eval(item.action), commandPalette.isOpen = false) : null"
                               class="flex items-center gap-3 p-3 rounded-lg cursor-pointer transition-colors"
                               :class="index === commandPalette.selectedIndex ? 'bg-primary/10 text-primary' : 'hover:bg-base-200'">
                                <span class="text-xs font-mono px-1.5 py-0.5 rounded bg-base-200 text-base-content/60" x-text="item.category"></span>
                                <span class="flex-1" x-text="item.title"></span>
                                <svg x-show="item.url" class="w-4 h-4 text-base-content/30" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </a>
                        </template>
                    </div>
                </template>
            </div>
            <div class="p-3 border-t border-base-300 flex gap-4 text-xs text-base-content/50">
                <span><kbd class="kbd kbd-xs">↑↓</kbd> Navigate</span>
                <span><kbd class="kbd kbd-xs">↵</kbd> Open</span>
                <span><kbd class="kbd kbd-xs">esc</kbd> Close</span>
            </div>
        </div>
    </div>

    <!-- Keyboard Shortcuts Help Modal -->
    <dialog id="shortcuts-modal" class="modal">
        <div class="modal-box max-w-md">
            <h3 class="font-bold text-lg mb-4">Keyboard Shortcuts</h3>
            <div class="overflow-x-auto">
                <table class="table table-sm">
                    <tbody>
                        <tr><td><kbd class="kbd kbd-sm">⌘</kbd> + <kbd class="kbd kbd-sm">K</kbd></td><td>Command Palette</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">⌘</kbd> + <kbd class="kbd kbd-sm">S</kbd></td><td>Save Entry</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">?</kbd></td><td>Show Shortcuts</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">Esc</kbd></td><td>Close Modal / Palette</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">g</kbd> then <kbd class="kbd kbd-sm">d</kbd></td><td>Go to Dashboard</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">g</kbd> then <kbd class="kbd kbd-sm">c</kbd></td><td>Go to Content Types</td></tr>
                        <tr><td><kbd class="kbd kbd-sm">g</kbd> then <kbd class="kbd kbd-sm">m</kbd></td><td>Go to Media</td></tr>
                    </tbody>
                </table>
            </div>
            <div class="modal-action">
                <form method="dialog"><button class="btn btn-sm">Close</button></form>
            </div>
        </div>
        <form method="dialog" class="modal-backdrop"><button>close</button></form>
    </dialog>

    <!-- Toast Notification Container -->
    <div class="fixed bottom-6 right-6 z-[200] space-y-3 w-80">
        <template x-for="toast in toasts" :key="toast.id">
            <div x-show="true"
                 x-transition:enter="transition ease-out duration-300"
                 x-transition:enter-start="opacity-0 translate-x-8"
                 x-transition:enter-end="opacity-100 translate-x-0"
                 x-transition:leave="transition ease-in duration-200"
                 x-transition:leave-start="opacity-100"
                 x-transition:leave-end="opacity-0"
                 class="alert shadow-lg relative overflow-hidden"
                 :class="{
                     'alert-success': toast.type === 'success',
                     'alert-error': toast.type === 'error',
                     'alert-warning': toast.type === 'warning',
                     'alert-info': toast.type === 'info'
                 }">
                <span x-text="toast.message"></span>
                <button @click="toasts = toasts.filter(t => t.id !== toast.id)" class="btn btn-xs btn-ghost">✕</button>
                <div class="absolute bottom-0 left-0 h-1 bg-current opacity-30 transition-all duration-100"
                     :style="'width: ' + toast.progress + '%'"></div>
            </div>
        </template>
    </div>

    <!-- Export Section for additional scripts -->
    #export("scripts")
</body>
</html>